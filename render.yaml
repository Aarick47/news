services:
  - type: web
    name: smart-news-digest-backend
    env: node
    buildCommand: npm run postinstall
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
      - key: PORT
        value: 10000
    plan: free
