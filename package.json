{
    "name": "news-deployment-proxy",
    "version": "1.0.0",
    "description": "Proxy to handle deployment for my-project-backend",
    "scripts": {
        "postinstall": "cd my-project-backend && npm install",
        "start": "cd my-project-backend && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}